import { defineCliConfig } from 'sanity/cli'

export default defineCliConfig({
    api: {
        projectId: '4x3yyhbh',
        dataset: 'production',
    },
    // Hosted at https://opencoregroup.sanity.studio
    studioHost: 'opencoregroup',
})
