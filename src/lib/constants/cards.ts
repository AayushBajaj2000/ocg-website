// Shared by every content card grid (blog, resources).
export const CARD_GRID_SKELETON_COUNT = 6;

/** Cards in the first desktop row load eagerly; the first one is the LCP candidate. */
export const CARD_GRID_EAGER_IMAGE_COUNT = 3;

export const CARD_GRID_IMAGE_SIZES = "(min-width: 64rem) 33vw, (min-width: 48rem) 50vw, 100vw";
