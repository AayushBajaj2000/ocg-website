# Fail loudly on a wrong Node version instead of producing a broken install.
engine-strict=true

# Keep pnpm's default strict/isolated node_modules — do NOT add shamefully-hoist.
# If a package turns out to read an undeclared dependency, add a narrow
# public-hoist-pattern[]=<that-package> line here instead of hoisting everything.
